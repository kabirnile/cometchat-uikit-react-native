name: Build Android APK

on:
  push:
    branches: [ v5, main, master ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Sample App Dependencies
        run: |
          cd examples/SampleApp
          npm install --legacy-peer-deps

      - name: Build Android APK
        run: |
          cd examples/SampleApp/android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: cometchat-whatsapp-release-apk
          path: examples/SampleApp/android/app/build/outputs/apk/release/app-release.apk
